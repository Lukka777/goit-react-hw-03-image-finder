import React from "react";
import { StyledBeatLoader } from "./styles";

const Spinner = () => {
    return (
        <StyledBeatLoader color= "#D7366E"  size={50} />
    );
};

export default Spinner;

